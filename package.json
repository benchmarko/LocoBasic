{
  "name": "locobasic",
  "version": "0.0.1",
  "description": "# LocoBasic - Loco BASIC",
  "dependencies": {
    "@types/node": "^22.9.0",
    "mkdirp": "^3.0.1",
    "ohm-js": "^17.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "@types/eslint__js": "^8.42.3",
    "eslint": "^9.14.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.13.0"
  },
  "scripts": {
    "lint": "eslint .",
    "clean": "rm -rf dist/*",
    "copy": "cp src/*.html dist/ && mkdirp -p dist/src && cp -r src/*.ts dist/src",
    "build": "tsc --build && npm run copy"
  }
}
